#!/usr/bin/env ruby
$:.unshift File.expand_path('../../lib', __FILE__)
require 'bubs'

if $stdin.stat.size > 0
  args = $stdin.read
else
  args = ARGV.join(' ')
end

text = Bubs.convert(args)
Bubs.copy(text)
puts text
